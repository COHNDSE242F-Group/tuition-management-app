<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Gradient border layer (only bottom corners rounded) -->
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:startColor="#3a6073"
                android:endColor="#3a7bd5"
                android:angle="0" />
            <corners
                android:bottomLeftRadius="16dp"
                android:bottomRightRadius="16dp"
                android:topLeftRadius="0dp"
                android:topRightRadius="0dp"/>
        </shape>
    </item>

    <!-- Inner white layer with inset to simulate border -->
    <item android:left="2dp" android:top="0dp" android:right="2dp" android:bottom="2dp">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF"/>
            <corners
                android:bottomLeftRadius="14dp"
                android:bottomRightRadius="14dp"
                android:topLeftRadius="0dp"
                android:topRightRadius="0dp"/>
        </shape>
    </item>

</layer-list>